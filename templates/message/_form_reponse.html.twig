{{ form_start(form, {'attr': {'class': 'placeho'}}) }}

  
        {{ form_widget(form.message, {'attr': {'placeholder': "Votre réponse"}}) }}
   
    
    <button class="btn btn-success">{{ button_label|default('Envoyer') }}</button>
{{ form_end(form) }}