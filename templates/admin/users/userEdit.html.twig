{% extends 'base.html.twig' %}

{% block title %}Modifier l'utilisateur{% endblock %}

{% block body %}
<div class="container" border style="text-align:center;">
    <h1>Modifier l'utilisateur</h1>

    {{ include('admin/users/_form.html.twig', {'button_label': 'Modifier'}) }}
    {{ include('admin/users/_delete_form.html.twig') }}
    <a class="btn btn-info" href="{{ path('user_index') }}">Retour à la liste</a>
</div>
{% endblock %}